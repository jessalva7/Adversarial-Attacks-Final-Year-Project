March 8 :

We referred another github link : https://github.com/adrianodennanni/pre-trained-nn-benchmark
The first model we used gave poor accuracy. So we tried a pretrained model on our dataset.
It gave above 96% accuracy for each character. 

March 19:

Attacking was done on captcha and it was successful. But the generated dataset posed an issue of overfitting so we generated a new dataset and plan to try training woith it.
